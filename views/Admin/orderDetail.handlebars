<div>
		<div class="jumbotron">
			<b><font size="5">Order Details</font></b>
		<table class="table table-striped table-bordered table-sm" cellspacing="0" width="60%">
		<thead class="thead-dark">
			<tr>
				<th>ItemName</th>
				<th>ItemPrice</th>
				<th>ItemQty</th>
				<th>SubTotal</th>
			</tr>
		</thead>
		<tbody>
				{{#each data}}
					<tr>
						<td>{{this.order_ItemName}}</td>
						<td>{{this.order_ItemPrice}}</td>
						<td>{{this.order_ItemQty}}</td>
						<td>{{this.order_subTotal}}</td>
					</tr>
				{{/each}}
			</tbody>
		<tfooter>
				<tr><td>
				<h3>Total = ${{orderTotal}}</h3>
				</td></tr>
		</tfooter>
		</table>
		<form action='/admin/customerlist'>
    		<input type="submit" class='btn btn-dark btn-lg' value="Go Back" />
		</form>
	</div>
</div>